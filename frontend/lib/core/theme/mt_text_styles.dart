import 'package:flutter/material.dart';

class MtTextStyles {
  // Display
  static const TextStyle displayLg = TextStyle(
    fontSize: 48,
    fontWeight: FontWeight.w800,
    height: 1.2,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Headings
  static const TextStyle h1 = TextStyle(
    fontSize: 28,
    fontWeight: FontWeight.w700,
    height: 1.3,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle h2 = TextStyle(
    fontSize: 22,
    fontWeight: FontWeight.w700,
    height: 1.4,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle h3 = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w700,
    height: 1.4,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Section label (ALL CAPS)
  static const TextStyle sectionLabel = TextStyle(
    fontSize: 11,
    fontWeight: FontWeight.w700,
    height: 1.5,
    letterSpacing: 0.8,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Body text
  static const TextStyle bodyLg = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w400,
    height: 1.5,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle bodyMd = TextStyle(
    fontSize: 14,
    fontWeight: FontWeight.w400,
    height: 1.5,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle bodySm = TextStyle(
    fontSize: 12,
    fontWeight: FontWeight.w400,
    height: 1.5,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Label text (for buttons, chips)
  static const TextStyle labelLg = TextStyle(
    fontSize: 14,
    fontWeight: FontWeight.w600,
    height: 1.4,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle labelMd = TextStyle(
    fontSize: 12,
    fontWeight: FontWeight.w600,
    height: 1.4,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle labelSm = TextStyle(
    fontSize: 10,
    fontWeight: FontWeight.w700,
    height: 1.4,
    letterSpacing: 0.6,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Data/number text
  static const TextStyle dataBig = TextStyle(
    fontSize: 28,
    fontWeight: FontWeight.w700,
    height: 1.2,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle dataMd = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.w700,
    height: 1.2,
    // fontFamily: 'Inter', // Use system font fallback
  );

  static const TextStyle dataSm = TextStyle(
    fontSize: 14,
    fontWeight: FontWeight.w600,
    height: 1.2,
    // fontFamily: 'Inter', // Use system font fallback
  );

  // Timer/Countdown
  static const TextStyle timer = TextStyle(
    fontSize: 13,
    fontWeight: FontWeight.w600,
    height: 1.2,
    fontFamily: 'monospace',
  );

  // Bangla text variants (using Kalpurush font)
  static const TextStyle bodyLgBangla = TextStyle(
    fontSize: 16,
    fontWeight: FontWeight.w400,
    height: 1.5,
    fontFamily: 'Kalpurush',
  );

  static const TextStyle bodySmBangla = TextStyle(
    fontSize: 12,
    fontWeight: FontWeight.w400,
    height: 1.5,
    fontFamily: 'Kalpurush',
  );

  static const TextStyle labelLgBangla = TextStyle(
    fontSize: 14,
    fontWeight: FontWeight.w600,
    height: 1.4,
    fontFamily: 'Kalpurush',
  );
}
